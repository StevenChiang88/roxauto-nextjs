/**
 * Full-viewport decorative background rendered behind all content.
 * Hand-crafted per style branch (gradient meshes, glass blobs, ...).
 * The neutral base renders nothing.
 */
export default function BackgroundDecor() {
  return null;
}
